name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.11', '3.12']
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install core dependencies (no heavy ML deps)
        run: |
          pip install --quiet fastapi uvicorn pdfplumber python-multipart requests pyyaml pytest httpx

      - name: Run tests
        run: pytest backend/tests -q
