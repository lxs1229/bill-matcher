# Bill Matcher · 架构设计说明

面向理解和使用者的架构文档，可直接分享。

## 一、解决的问题

用户有**多张消费收据**（纸质拍照、电子PDF、邮件截图都有）和一张**银行卡月账单**（同样多格式），想要自动把**每张收据 ↔ 对应的银行流水交易**配对，并给每笔消费打上分类标签，供人工复核。

## 二、整体架构

```
                        ┌─────────────────────────────────────────────┐
                        │              数据输入层                      │
  收据(拍照/PDF/截图) ──┼──► 视觉AI(Qwen2.5-VL-7B 本地)              │
  银行账单(电子PDF)   ──┼──► pdfplumber (PDF文本解析)                 │
  银行账单(照片/扫描) ──┼──► PaddleOCR PP-StructureV3 (表格识别)       │
                        └─────────────────────────────────────────────┘
                                        │  输出结构化数据
                                        ▼
                        ┌─────────────────────────────────────────────┐
                        │            配对引擎 (核心)                   │
                        │  每张收据 × 每条交易，三因子打分：            │
                        │    金额(0.55) + 商户名模糊(0.30) + 日期(0.15) │
                        └─────────────────────────────────────────────┘
                                        │  置信分 0~1
                                        ▼
                        ┌─────────────────────────────────────────────┐
                        │            人工复核界面                       │
                        │  ≥0.80 自动配对 │ 0.55~0.80 复核 │ <0.55 不匹配│
                        │  用户点 确认/拒绝                              │
                        └─────────────────────────────────────────────┘
                                        │
                                        ▼
                        ┌─────────────────────────────────────────────┐
                        │  DeepSeek v4 分类 + 月度汇总报表              │
                        └─────────────────────────────────────────────┘
```

**技术栈**：FastAPI + SQLite（后端）、单页 HTML/JS（前端）、本地视觉（Qwen2.5-VL-7B / PaddleOCR）、DeepSeek v4（分类）。前端是 Web 技术，之后可包成桌面版（Tauri）复用代码。

所有 AI 供应商可在 **`config.yaml`** 中切换（视觉识别 + 分类各一个）。

## 三、关键设计决策：三类输入，三种提取方案

| 输入 | 提取方案 | 为什么 |
|------|---------|--------|
| 收据（少字段、自由文本） | **Qwen2.5-VL-7B** 本地视觉模型 | 字段少、信息简单，7B 视觉模型提取准确，且**免费、私密**（银行数据不出机器） |
| 银行账单·电子PDF | **pdfplumber** 直接解析文本 | 数字版 PDF 文本解析是**确定性**的，100% 准确，根本不需要视觉 |
| 银行账单·照片/扫描 | **PaddleOCR PP-StructureV3** 表格识别 | 扫描件才需要 OCR，且必须用专业**表格识别**（按网格切分单元格） |

### ⚠️ 最重要的教训（已实测验证）

> **绝不能用 7B 视觉模型去啃密集的银行表格。**

实测 Qwen2.5-VL-7B 读单张收据很准，但读银行月账单时**金额大面积幻觉**（如 22.83 被读成 124.50、40.48 被读成 10.00）。金额错，配对就全崩。所以**表格数据必须走确定性的 OCR 表格识别或 PDF 文本解析**，不能用生成式视觉模型。

另外验证出：PaddleOCR 普通识别在没有表格线、列间距窄的图上会列黏连（金额并进摘要），但**带网格线的表格用 PP-StructureV3 能 100% 精确读出行和金额**。

## 四、配对引擎（三因子打分）

对「每张收据 × 每条银行交易」计算一个**可解释的置信分**，供人工快速复核：

```
置信分 = 0.55×金额分 + 0.30×商户名分 + 0.15×日期分

金额分   : 差异 ≤ ±0.02 记满分，线性衰减，超 ±1.00 直接判不配
商户名分 : 归一化后模糊匹配（去大小写/去音标/去停用词，
          Jaccard + 顺序相似度）
日期分   : 按天数差 1/(1+days)，容忍收据日与银行入账日的偏差
```

分档策略：
- **≥0.80 自动配对**：直接进确认列表
- **0.55~0.80 待复核**：金额商户大概率对，但日期或名称有出入，人扫一眼确认
- **<0.55 不匹配**：单独列出，同时自动报告「无对应收据的银行交易」（如 ATM 取现）

已配对的交易自动标记避免重复使用，未配对的收据和未配对的交易都会单独报告。

## 五、分类与汇总

- **DeepSeek v4** 把已确认的支出批量分类（餐饮/超市购物/交通/居住水电/娱乐订阅/数码/医疗/旅行/转账/其他），输出 JSON。类别可在 config.yaml 自定义。
- 按类别汇总月度支出，前端展示统计卡片。

## 六、人工复核是设计核心

AI 只负责**给出高置信的建议**，最终由用户确认——这是账单对账这类场景保证准确率的正确做法，而不是让 AI 全自动拍板。
